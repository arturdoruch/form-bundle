{% extends "ArturDoruchFormBundle:Form:bootstrap_3.html.twig" %}

{%- block form_start -%}
    {% set attr = attr|merge({class: (attr.class|default('') ~ ' form-inline')|trim}) %}
    {{- parent() -}}
{%- endblock form_start -%}